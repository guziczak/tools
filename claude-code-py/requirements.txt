anthropic>=0.39.0
rich>=13.7.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
flask>=3.0.0

# Optional: For OAuth token exchange (bypasses Cloudflare challenge)
# Install with: pip install cloudscraper
# This is a legitimate library for automated requests that need to pass Cloudflare protection
cloudscraper>=1.2.71

# Optional: For FULL AUTO sessionKey extraction from claude.ai
# Install with: pip install selenium webdriver-manager
# Enables automatic browser automation - you just sign in, Python does the rest!
selenium>=4.15.0
webdriver-manager>=4.0.0
