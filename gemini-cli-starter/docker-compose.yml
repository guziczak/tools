services:
  gemini-cli:
    build:
      context: .
      dockerfile: Dockerfile
    image: gemini-cli-container:latest
    container_name: gemini-persistent
    hostname: gemini-container
    stdin_open: true
    tty: true

    # Root user for mount namespace capabilities
    user: root

    # Security capabilities for namespace operations
    cap_add:
      - SYS_ADMIN
      - SETUID
      - SETGID
    security_opt:
      - apparmor:unconfined
      - seccomp:unconfined

    # Volume configuration
    volumes:
      # Host filesystem mounts - adjust based on OS
      # For Windows with WSL 2 backend, use /mnt/c format
      - type: bind
        source: /mnt/c
        target: /host_c
        read_only: true

      # Uncomment if you have D: drive (use /mnt/d format)
      # - type: bind
      #   source: /mnt/d
      #   target: /host_d
      #   read_only: true

      # For Linux/Mac, use this instead:
      # - type: bind
      #   source: /
      #   target: /host
      #   read_only: true

      # Shared volume for tools installed across sessions
      - type: volume
        source: gemini-shared-tools
        target: /opt/gemini-shared

      # Shared system directories for apt packages
      - type: volume
        source: gemini-apt-cache
        target: /var/cache/apt

      - type: volume
        source: gemini-apt-lib
        target: /var/lib/apt

      # Persistent /usr/local for global npm/pip installations
      - type: volume
        source: gemini-usr-local
        target: /usr/local

      # Session tracking volume for session files
      # On Windows, Docker Desktop handles the path translation
      - type: bind
        source: ${TEMP:-/tmp}/gemini-sessions
        target: /var/run/gemini-sessions
        bind:
          create_host_path: true

    # Working directory
    working_dir: /home/gemini

    # Restart policy
    restart: unless-stopped

    # Add init for proper signal handling
    init: true

    # Add tmpfs for temporary files
    tmpfs:
      - /tmp:size=1G,mode=1777

    # Remove command - supervisord will be started by Dockerfile CMD

    # Environment variables
    environment:
      - DOCKER_BUILDKIT=1
      - COMPOSE_DOCKER_CLI_BUILD=1
      - TZ=${TZ:-UTC}

    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '${GEMINI_CPU_LIMIT:-4}'
          memory: ${GEMINI_MEMORY_LIMIT:-8G}
        reservations:
          cpus: '${GEMINI_CPU_RESERVATION:-1}'
          memory: ${GEMINI_MEMORY_RESERVATION:-2G}

    # Enhanced health check - verify supervisord and session manager are functioning
    healthcheck:
      test: ["CMD-SHELL", "supervisorctl status gemini-session-manager | grep -q RUNNING && [ -f /var/run/gemini-cleanup.health ] && [ $(($(date +%s) - $(cat /var/run/gemini-cleanup.health))) -lt 120 ]"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
        labels: "service=gemini-cli"

# Volume definitions
volumes:
  gemini-shared-tools:
    name: gemini-shared-tools
  gemini-apt-cache:
    name: gemini-apt-cache
  gemini-apt-lib:
    name: gemini-apt-lib
  gemini-usr-local:
    name: gemini-usr-local
